Para correr exitosamente los archivos, hay que asegurarnos de tener instaladas las librerías de Python que se usan. Esto se puede hacer con pip en la terminal de macOS o en el command prompt de Windows, corriendo los siguientes comandos:

pip install numpy
pip install matplotlib
pip install Pillow
